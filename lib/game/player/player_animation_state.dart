/// Enum representing player animation states.
enum PlayerAnimationState {
  /// Player standing still.
  idle,

  /// Player moving via joystick control.
  walk,

  /// Player interacting with a building or object.
  interact,
}
