/// Level configuration for Science subject domain.
class ScienceLevel {
  final String title;
  final int levelIndex;

  const ScienceLevel({
    required this.title,
    required this.levelIndex,
  });
}
