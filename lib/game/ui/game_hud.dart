import 'package:flame/components.dart';

/// Container component for managing game HUD elements, health bars, scores, and UI overlays.
class GameHud extends Component {
  // Prepared container for score displays, mini-maps, and status indicators.
}
